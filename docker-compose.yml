version: '3.8'

services:
  app:
    build: .
    ports:
      - '3000:3000'

    env_file:
      - .env

    # Graceful shutdown configuration
    stop_grace_period: 30s # Time to wait for SIGTERM before sending SIGKILL
    restart: unless-stopped

    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:3000/health/liveness']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
